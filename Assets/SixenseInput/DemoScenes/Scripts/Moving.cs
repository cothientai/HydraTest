using UnityEngine;
using System.Collections;

public class Moving : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
//		if ( SixenseInput.Controllers[2] != null )
//		{
//			var angH = SixenseInput.Controllers[2].JoystickX * 60;
//  			var angV = SixenseInput.Controllers[2].JoystickY * 45;
//			Debug.Log("X joystick position of controller" + SixenseInput.Controllers[2].JoystickX);
//  			transform.localEulerAngles = new Vector3(angV, angH, 0);
//		}
	}
}
